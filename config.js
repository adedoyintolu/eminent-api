const config = {
  db: {
    /* don't expose password or any sensitive info, done only for demo */
    host: "db4free.net",
    user: "eminent",
    password: "doyin1929",
    database: "eminentdb",
  },
  listPerPage: 10,
};
module.exports = config;